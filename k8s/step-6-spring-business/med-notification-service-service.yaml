apiVersion: v1
kind: Service
metadata:
  name: med-notification-service
  namespace: medback
  labels:
    app.kubernetes.io/name: med-notification-service
    app.kubernetes.io/component: business
    app.kubernetes.io/part-of: medback-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: med-notification-service
    app.kubernetes.io/component: business
  ports:
    - name: http
      port: 8085
      targetPort: http
      protocol: TCP
