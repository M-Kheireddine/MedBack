spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/MED_USER_DB
    username: postgres
    password: MED_USER_PASSWORD
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true