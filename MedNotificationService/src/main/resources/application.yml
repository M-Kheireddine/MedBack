spring:
  application:
    name: MED_NOTIFICATION_SERVICE
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
    discovery:
      enabled: true

server:
  port: 8085
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health, refresh

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka}